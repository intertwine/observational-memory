# Archive

This folder keeps old implementation plans and status reports. They are useful history, but they are not the current user guide.

Current docs live one level up in `docs/`.

## Folders

- `cluster/`: old OM Cluster status reports.
- `plans/`: completed implementation plans.

## Active Plan

The Hermes plugin plan is still active and remains in `plans/hermes-first-class-plugin.md`.

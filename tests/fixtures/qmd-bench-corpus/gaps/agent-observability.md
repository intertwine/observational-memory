# Agent Observability Gap

A recurring issue is the agent visibility gap between local work, hidden hook behavior, and review context.
The fix is to surface status plainly in `om status`, `om doctor`, and maintainer docs so operators can tell what the agent actually sees.

This gap cuts across search, scheduling, and review workflows rather than living in one subsystem.

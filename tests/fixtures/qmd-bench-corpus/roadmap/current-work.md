# QMD Upgrade Rollout

## Status Snapshot

The QMD 2.1 integration first slice is already merged.
The next slice focuses on maintainer benchmarking with a deterministic repo-local corpus, a `qmd bench` fixture, and Make targets for repeatable comparisons.

## Why This Exists

Search backend defaults should only change after measured retrieval comparisons instead of anecdotal impressions.

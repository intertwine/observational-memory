Ship Observational Memory v0.6.5 with first-class `om login` flows for OpenAI ChatGPT (subscription) and xAI Grok (SuperGrok), e2e-proven against live accounts, fully documented, with a publish-ready article in the code-and-context-ghost repo.

Authoritative plan: `plans/v0.6.5-account-auth.md` — read first; do not re-litigate design decisions there.

Done when ALL hold:

1. New `om` providers `openai-chatgpt` and `xai-oauth` land, plus a metered `xai` provider for API-key fallback. Existing `anthropic`/`openai` providers behave identically. Provider auto-resolution follows the five-rule order in the plan.

2. `om login`, `om login {openai-chatgpt|xai-oauth|--api-key …|--import}`, `om logout`, `om auth status`, and `om auth refresh` are implemented per the plan's CLI section. `om install` interactive flow and `om doctor` surface the new options as the plan specifies.

3. Auth store lives at `~/.config/observational-memory/auth.json` (0600, file-locked). Tokens never logged; stdout/log redaction asserted by tests. Never writes to `~/.codex/` or `~/.grok/`.

4. xAI loopback flow ports Hermes upstream verbatim (`nousresearch/hermes-agent` `hermes_cli/auth.py` commit `5fd3676`): `plan=generic` + `referrer=observational-memory` authorize params, `code_challenge` echoed at token step, CORS handler on the loopback server, manual-paste fallback for SSH, `*.x.ai` host-pinning on discovery endpoints AND inference base URL, `xai_oauth_tier_denied` mapping on HTTP 403. Cite upstream file path + commit in each ported module header.

5. OpenAI ChatGPT device-code flow ports the Hermes Codex flow (`_codex_device_code_login`, `refresh_codex_oauth_pure`, expiry/skew logic).

6. Test suite green: `make check` clean (ruff check + ruff format --check + pytest) on 3.11/3.12/3.13. New `tests/auth/` covers store round-trip + lock contention, PKCE, both refresh paths, 401-triggered single-retry, host-pinning rejection, base-URL exfiltration rejection, tier-denied detection, manual-paste mode, state/code mismatch.

7. E2E proof captured in `docs/RELEASE-0.6.5.md` as a transcript appendix: live `om login openai-chatgpt` and live `om login xai-oauth` (real subscriptions), then `om observe` and `om reflect` each completing a real run against subscription-backed inference, with redacted token tails shown and the resulting `observations.md` / `reflections.md` diffs summarized. No synthetic mocks in this section.

8. Docs updated: `docs/configuration.md` gains the three new provider sections + the "Save money: use your subscription" explainer with cost-comparison table; `docs/install.md` mentions `om login`; README gets a one-line v0.6.5 bullet (keep README short); `docs/RELEASE-0.6.5.md` covers providers/CLI/env/risks.

9. Article draft in the `code-and-context-ghost` repo (sibling checkout under `~/experiments/`) as a new post (use the repo's existing post format). Title direction: "Stop paying per-token: use your ChatGPT or SuperGrok subscription with Observational Memory." Covers the problem (per-call cost on cheap-feeling features), the fix (subscription auth), the security model (host-local store, host pinning, no writes to upstream CLI files), credit to Hermes for the xAI flow groundwork, and a 30-second quickstart. Publish-ready prose; do not publish.

10. Release boundary respected per CLAUDE.md: do NOT bump version, tag, publish, update Homebrew, or change CLAUDE.md "Current Release Context" without explicit user approval at the end. Stop after release notes are written and report ready-to-ship status.

Out of scope: token budgets (#51), Anthropic subscription auth, cluster-syncing tokens, first-party OAuth client registration with OpenAI/xAI.

If you discover the plan is wrong, update the plan file in the same PR with a "Plan amendment" section and explain why — do not silently diverge.
